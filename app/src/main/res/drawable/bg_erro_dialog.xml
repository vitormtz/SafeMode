<?xml version="1.0" encoding="utf-8"?>
<!-- Fundo escuro para diálogos e mensagens de bloqueio -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fundo escuro (azul principal escuro) -->
    <solid android:color="#252527" />

    <!-- Bordas arredondadas -->
    <corners android:radius="20dp" />

    <!-- Borda mais clara para destacar -->
    <stroke
        android:width="2dp"
        android:color="#252527" />

</shape>