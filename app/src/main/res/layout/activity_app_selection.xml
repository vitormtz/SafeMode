<?xml version="1.0" encoding="utf-8"?>
<!-- Layout da tela para selecionar apps - VERSÃO COM CONTAINER -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1F3A5F">

    <View
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:background="#1F3A5F" />

    <!-- Conteúdo principal -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <!-- ✨ NOVO: Container LinearLayout para os Apps -->
        <LinearLayout
            android:id="@+id/container_apps"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@drawable/card_background"
            android:padding="20dp"
            android:elevation="4dp"
            android:visibility="gone">

            <!-- ✨ MOVIDO: Título do Container -->
            <TextView
                android:id="@+id/container_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Selecionar Apps para Bloquear"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#212121"
                android:layout_marginBottom="8dp"
                android:drawablePadding="8dp" />

            <!-- ✨ MOVIDO: Descrição do que fazer -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Marque os aplicativos que devem ser bloqueados quando você estiver fora da área permitida."
                android:textSize="14sp"
                android:textColor="#666666"
                android:lineSpacingExtra="2dp"
                android:layout_marginBottom="16dp" />

            <!-- ✨ NOVO: Linha separadora -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#E0E0E0"
                android:layout_marginBottom="16dp" />

            <!-- Lista de Apps DENTRO do Container -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_apps"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:scrollbars="vertical"
                android:clipToPadding="false"
                android:padding="4dp" />

        </LinearLayout>

        <!-- Indicador de Carregamento -->
        <LinearLayout
            android:id="@+id/loading_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="visible"
            android:background="@drawable/card_background"
            android:padding="32dp">

            <ProgressBar
                android:id="@+id/progress_loading"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginBottom="16dp"
                android:indeterminateTint="#2196F3" />

            <TextView
                android:id="@+id/text_loading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Carregando aplicativos..."
                android:textSize="16sp"
                android:textColor="#666666"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Mensagem quando não há apps -->
        <LinearLayout
            android:id="@+id/layout_empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone"
            android:padding="32dp"
            android:background="@drawable/card_background">

            <ImageView
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:src="@drawable/ic_apps"
                android:layout_marginBottom="16dp"
                app:tint="#CCCCCC" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="😔 Nenhum aplicativo encontrado"
                android:textSize="18sp"
                android:textColor="#666666"
                android:textStyle="bold"
                android:layout_marginBottom="8dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Não foi possível carregar a lista de aplicativos instalados no seu dispositivo."
                android:textSize="14sp"
                android:textColor="#999999"
                android:gravity="center"
                android:lineSpacingExtra="2dp" />

            <!-- ✨ NOVO: Botão para tentar novamente -->
            <Button
                android:id="@+id/btn_retry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🔄 Tentar Novamente"
                android:textColor="#2196F3"
                android:background="@drawable/button_outline"
                android:layout_marginTop="16dp"
                android:padding="12dp"
                android:textAllCaps="false" />

        </LinearLayout>

    </FrameLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="#1F3A5F" />

</LinearLayout>