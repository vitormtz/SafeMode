<?xml version="1.0" encoding="utf-8"?><!-- Layout da tela de seleção de aplicativos para bloquear -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1F3A5F"
    android:orientation="vertical">

    <!-- Espaçador superior (status bar) -->
    <View
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:background="#1F3A5F" />

    <!-- Container principal com múltiplos estados -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <!-- Container com lista de apps -->
        <LinearLayout
            android:id="@+id/container_apps"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/card_background"
            android:elevation="4dp"
            android:orientation="vertical"
            android:padding="20dp"
            android:visibility="gone">

            <!-- Título da tela -->
            <TextView
                android:id="@+id/container_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:drawablePadding="8dp"
                android:text="Selecionar Apps para Bloquear"
                android:textColor="#212121"
                android:textSize="20sp"
                android:textStyle="bold" />

            <!-- Descrição explicativa -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:lineSpacingExtra="2dp"
                android:text="Marque os aplicativos que devem ser bloqueados quando você estiver fora da área permitida."
                android:textColor="#666666"
                android:textSize="14sp" />

            <!-- Divisor horizontal -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginBottom="16dp"
                android:background="#E0E0E0" />

            <!-- RecyclerView com lista de aplicativos -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_apps"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:clipToPadding="false"
                android:padding="4dp"
                android:scrollbars="vertical" />

        </LinearLayout>

        <!-- Layout de carregamento -->
        <LinearLayout
            android:id="@+id/loading_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/card_background"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="32dp"
            android:visibility="visible">

            <!-- Indicador de progresso -->
            <ProgressBar
                android:id="@+id/progress_loading"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginBottom="16dp"
                android:indeterminateTint="#2196F3" />

            <!-- Texto de carregamento -->
            <TextView
                android:id="@+id/text_loading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Carregando aplicativos..."
                android:textColor="#666666"
                android:textSize="16sp"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Layout de lista vazia (erro) -->
        <LinearLayout
            android:id="@+id/layout_empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/card_background"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="32dp"
            android:visibility="gone">

            <!-- Ícone de apps -->
            <ImageView
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_marginBottom="16dp"
                android:src="@drawable/ic_apps"
                app:tint="#CCCCCC" />

            <!-- Mensagem principal de erro -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="😔 Nenhum aplicativo encontrado"
                android:textColor="#666666"
                android:textSize="18sp"
                android:textStyle="bold" />

            <!-- Mensagem secundária de erro -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:lineSpacingExtra="2dp"
                android:text="Não foi possível carregar a lista de aplicativos instalados no seu dispositivo."
                android:textColor="#999999"
                android:textSize="14sp" />

            <!-- Botão para tentar novamente -->
            <Button
                android:id="@+id/btn_retry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/button_outline"
                android:padding="12dp"
                android:text="🔄 Tentar Novamente"
                android:textAllCaps="false"
                android:textColor="#2196F3" />

        </LinearLayout>

    </FrameLayout>

    <!-- Espaçador inferior (navigation bar) -->
    <View
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="#1F3A5F" />

</LinearLayout>