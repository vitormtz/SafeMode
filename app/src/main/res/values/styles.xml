<?xml version="1.0" encoding="utf-8"?>
<!-- styles.xml - VERSÃO CORRIGIDA para barra de navegação -->
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🎨 TEMA PRINCIPAL - CORRIGIDO PARA BARRAS DO SISTEMA -->
    <style name="Theme.SafeMode" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/primary_dark_blue</item>
        <item name="colorPrimaryDark">@color/primary_dark_blue</item>
        <item name="colorAccent">@color/action_blue</item>

        <!-- Fundo da aplicação -->
        <item name="android:windowBackground">@color/background_app</item>

        <!-- ✅ CORREÇÃO: Configurações da barra de status -->
        <item name="android:statusBarColor">@color/primary_dark_blue</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- ✅ CORREÇÃO: Configurações da barra de navegação -->
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>

        <!-- Texto da action bar -->
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- 🎨 TEMA ALTERNATIVO - Para telas que precisam de fundo mais claro -->
    <style name="Theme.SafeMode.Light" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary_dark_blue</item>
        <item name="colorPrimaryDark">@color/primary_dark_blue</item>
        <item name="colorAccent">@color/action_blue</item>
        <item name="android:windowBackground">@color/background_light</item>

        <!-- Manter barras escuras mesmo com fundo claro -->
        <item name="android:statusBarColor">@color/primary_dark_blue</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
    </style>

    <!-- 🎨 TEMA PARA ATIVIDADES FULLSCREEN (como a tela de bloqueio) -->
    <style name="Theme.SafeMode.Fullscreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>

        <!-- ✅ IMPORTANTE: Barras transparentes mas com ícones brancos -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>

        <!-- Permitir desenhar atrás das barras do sistema -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <!-- 🎨 ESTILOS PARA TÍTULOS -->
    <style name="TitleLarge">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/title_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="TitleMedium">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/title_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="TitleSmall">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/title_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <!-- 🎨 ESTILOS PARA TEXTO NORMAL -->
    <style name="TextPrimary">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/text_primary_dark</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="TextSecondary">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="TextCaption">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- 🎨 ESTILOS PARA BOTÕES -->
    <style name="ButtonPrimary" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:elevation">2dp</item>
    </style>

    <style name="ButtonSecondary" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_outline</item>
        <item name="android:textColor">@color/action_blue</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="ButtonWarning" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/warning_background</item>
        <item name="android:textColor">@color/warning_red</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- 🎨 ESTILOS PARA CARDS -->
    <style name="CardStyle">
        <item name="android:background">@drawable/card_background</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">16dp</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="CardStyleLarge">
        <item name="android:background">@drawable/card_background</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:padding">20dp</item>
        <item name="android:elevation">6dp</item>
    </style>

    <!-- 🎨 ESTILOS PARA SWITCH -->
    <style name="SwitchStyle" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="android:thumb">@drawable/switch_thumb</item>
        <item name="android:track">@drawable/switch_track</item>
        <item name="android:switchMinWidth">52dp</item>
        <item name="android:switchPadding">8dp</item>
    </style>

    <!-- 🎨 ESTILOS PARA SEEKBAR -->
    <style name="SeekBarStyle" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressDrawable">@drawable/seekbar_progress</item>
        <item name="android:thumb">@drawable/seekbar_thumb</item>
        <item name="android:minHeight">8dp</item>
        <item name="android:maxHeight">8dp</item>
    </style>

    <!-- 🎨 ESTILOS PARA EDITTEXT -->
    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:textColor">@color/text_primary_dark</item>
        <item name="android:textColorHint">@color/text_placeholder</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <!-- 🎨 ESTILOS PARA CHECKBOX -->
    <style name="CheckBoxStyle" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_selector</item>
        <item name="android:textColor">@color/text_primary_dark</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- 🎨 ESTILOS PARA DIVISORES -->
    <style name="DividerHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@drawable/divider_horizontal</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <!-- 🎨 ESTILOS PARA MENSAGENS -->
    <style name="MessageWarning">
        <item name="android:background">@drawable/warning_background</item>
        <item name="android:textColor">@color/warning_red</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="MessageSuccess">
        <item name="android:background">@drawable/success_background</item>
        <item name="android:textColor">@color/success_green</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- 🎨 ESTILOS PARA ÍCONES -->
    <style name="IconSmall">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <style name="IconMedium">
        <item name="android:layout_width">32dp</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <style name="IconLarge">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <!-- 🎨 TEMA PARA DIÁLOGOS -->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/primary_dark_blue</item>
        <item name="colorAccent">@color/action_blue</item>
        <item name="android:windowBackground">@drawable/card_background</item>
    </style>

    <!-- 🎨 ESTILO PARA TOOLBAR -->
    <style name="ToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/primary_dark_blue</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="android:elevation">4dp</item>
    </style>

</resources>